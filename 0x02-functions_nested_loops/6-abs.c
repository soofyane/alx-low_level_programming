#include "main.h"
/**
 * _abs - Write a function that computes the absolute value of an integer.
 *@c : number
 *
 * Return: absolute value of any number
 */
int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	else
	{
		return (-c);
	}
}
